# Lean dependency set for the Render web service (ensemble API only).
# Versions match the environment the models were trained/pickled with
# (numpy 2.x, sklearn 1.9.0) so the .pkl files load without ABI errors.
# torch / streamlit / plotly are excluded (unused by service/app.py).
fastapi==0.141.1
uvicorn[standard]==0.30.6
pandas==2.3.3
numpy>=2.0,<3
scikit-learn==1.9.0
xgboost==3.4.0
ta==0.11.0
yfinance==0.2.51
joblib>=1.3
requests==2.32.3
python-multipart>=0.0.10
# Auth & database (required by service routes)
sqlalchemy>=2.0,<3
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3
pyjwt>=2.8
bcrypt>=4.0
# Live NSE market data
nse>=4.0.0

